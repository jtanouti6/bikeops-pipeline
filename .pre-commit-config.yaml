repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.8
    hooks:
      - id: ruff           # lint + auto-fix (inclut tri d'import via "I")
      - id: ruff-format    # formateur (remplace Black)
